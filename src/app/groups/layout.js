// Main layout with navbar, footer, and sidebars for groups
import Layout from "@/components/layout/Layout";

export default function GroupsLayout({ children }) {
  return <Layout>{children}</Layout>;
}