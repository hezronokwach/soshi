// Notifications layout with sidebar navigation
import Layout from "../../components/layout/Layout";

export default function NotificationsLayout({ children }) {
  return <Layout>{children}</Layout>;
}
